// Mirrors backend/utils/workloadCalculator.js loadStatus values — shared
// between CreateTaskModal (assignee dropdown) and ProjectFormModal (team
// member picker) so both render the same Algorithmic Workload Balancer output.
export const LOAD_EMOJI = { LOW_LOAD: '🟢', OPTIMAL_LOAD: '🟡', OVERLOADED: '🔴' };
